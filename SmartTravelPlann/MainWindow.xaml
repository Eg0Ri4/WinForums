<Window x:Class="SmartTravelPlanner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Smart Travel Planner" Height="500" Width="800"
        Background="#F0F0F0">

	<Grid Margin="10">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>
		
		<Grid Grid.Row="0" Margin="0,0,0,15">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="Auto"/>
				<ColumnDefinition Width="300"/>
				<ColumnDefinition Width="Auto"/>
			</Grid.ColumnDefinitions>
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="Auto"/>
			</Grid.RowDefinitions>

			<TextBlock Grid.Row="0" Grid.Column="0" Text="Traveler Name:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,10,5"/>
			<TextBox x:Name="TravelerNameInput" Grid.Row="0" Grid.Column="1" Height="25" Margin="0,0,20,5" VerticalContentAlignment="Center"/>

			<TextBlock Grid.Row="1" Grid.Column="0" Text="Current Location:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,10,0"/>
			<TextBox x:Name="CurrentLocationInput" Grid.Row="1" Grid.Column="1" Height="25" VerticalContentAlignment="Center"/>
			<Button Click="CreateTraveler_Click" Grid.Column="2" Grid.Row="0" Grid.RowSpan="2"
                    Content="Create Traveler"
                    Width="120" Background="#C48E8E" Foreground="White" FontWeight="Bold"
                    BorderThickness="0"/>
		</Grid>
		<Grid Grid.Row="1" Margin="0,0,0,10">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="Auto"/>
				<ColumnDefinition Width="Auto"/>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="Auto"/>
			</Grid.ColumnDefinitions>

			<Button Click="LoadMap_Click" Grid.Column="0" Content="Load Map" Padding="15,5" Background="#C48E8E" Foreground="White" BorderThickness="0" Margin="0,0,20,0"/>
			<TextBlock Grid.Column="1" Text="Destination:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,10,0"/>
			<TextBox x:Name="DestinationInput" Grid.Column="2" Height="25" VerticalContentAlignment="Center" Margin="0,0,20,0"/>
			<Button Click="PlanRoute_Click" Grid.Column="3" Content="Plan Route" Padding="15,5" Background="#C48E8E" Foreground="White" BorderThickness="0"/>
		</Grid>
		<Grid Grid.Row="2" Margin="0,0,0,10">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="Auto"/>
			</Grid.ColumnDefinitions>

			<Border Grid.Column="0" Background="White" BorderBrush="#999" BorderThickness="1">
				<TextBlock x:Name="MapStatusText" Text="Map View Area" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#CCC" FontSize="20" FontWeight="Bold"/>
			</Border>

			<StackPanel Grid.Column="1" Margin="15,0,0,0" VerticalAlignment="Top" Width="120">
				<Button Click="Save_Click" Content="Save" Margin="0,0,0,10" Height="30" Background="#C48E8E" Foreground="White" BorderThickness="0"/>
				<Button Click="Load_Click" Content="Load" Margin="0,0,0,10" Height="30" Background="#C48E8E" Foreground="White" BorderThickness="0"/>
				<Button Click="ClearRoute_Click" Content="Clear Route" Margin="0,0,0,10" Height="30" Background="#C48E8E" Foreground="White" BorderThickness="0"/>
				<Button Click="Exit_Click" Content="Exit" Height="30" Background="#C48E8E" Foreground="White" BorderThickness="0"/>
			</StackPanel>
		</Grid>
		<StackPanel Grid.Row="3" Orientation="Horizontal">
			<TextBlock Text="Total Distance:" FontWeight="Bold"/>
			<TextBlock x:Name="DistanceText" Text=" -" Margin="5,0,0,0"/>
		</StackPanel>

	</Grid>
</Window>