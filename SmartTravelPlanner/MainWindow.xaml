<Window x:Class="SmartTravelPlanner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Smart Travel Planner" Height="600" Width="800"
        Background="#F5F5F5">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#262626"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="15,5"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="30"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="25"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="5,0"/>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Name:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,10,0"/>
            <TextBox x:Name="TravelerNameInput" Grid.Column="1"/>

            <TextBlock Grid.Column="3" Text="Location:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,10,0"/>
            <TextBox x:Name="CurrentLocationInput" Grid.Column="4"/>
        </Grid>

        <Button Grid.Row="1" Click="CreateTraveler_Click" 
                Content="Create Traveler" 
                HorizontalAlignment="Stretch" 
                Margin="0,0,0,15"/>

        <Separator Grid.Row="1" VerticalAlignment="Bottom" Margin="0,5,0,0"/>

        <Grid Grid.Row="2" Margin="0,10,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Destination:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,10,0"/>
            <TextBox x:Name="DestinationInput" Grid.Column="1" Margin="0,0,10,0"/>
            <Button Grid.Column="2" Click="PlanRoute_Click" Content="Plan Route" Width="120"/>
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,5">
            <TextBlock Text="Total Distance:" FontWeight="Bold" FontSize="14"/>
            <TextBlock x:Name="DistanceText" Text=" -" Margin="5,0,0,0" FontSize="14"/>
        </StackPanel>

        <Border Grid.Row="4" Background="White" BorderBrush="#999" BorderThickness="1" Margin="0,0,0,15">
            <Grid>
                <TextBlock x:Name="MapStatusText" Text="Map View" 
                           HorizontalAlignment="Center" VerticalAlignment="Center" 
                           Foreground="#CCC" FontSize="24" FontWeight="Bold"/>
            </Grid>
        </Border>

        <WrapPanel Grid.Row="5" HorizontalAlignment="Center" Orientation="Horizontal">
            <Button Click="LoadMap_Click" Content="Load Map"/>
            <Button Click="Save_Click" Content="Save Traveler"/>
            <Button Click="Load_Click" Content="Load Traveler"/>
            <Button Click="ClearRoute_Click" Content="Clear Route"/>
        </WrapPanel>

    </Grid>
</Window>